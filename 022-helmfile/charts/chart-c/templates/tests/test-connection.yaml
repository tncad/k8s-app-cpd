apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-c.fullname" . }}-test-connection"
  labels:
{{ include "chart-c.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chart-c.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
